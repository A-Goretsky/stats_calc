#include <iostream>

void mean(double[] list) {
	double sum;
	for (int x = 0; x < (int) list.size(), x++) {
		sum += list[x];
	}
	double res = sum / (int) list.size();
	std::cout << res << "\n";
}

void freq_list(double[] list) {
	std::cout << "Not Written Yet\n";
}

void freq_chart(double[] list) {
	std::cout << "Not Written Yet\n";
}
